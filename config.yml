pupServer:
  url: http://localhost
  db:
    path: C:\vPinball\PinUPSystem\PUPDatabase.db
    filter: ifnull(GameTheme, 1) <> 'Adult'

httpServer:
  port: 8080
  logFormat: dev
  logLevel: error

options:
  kioskMode: false
  defaultView: playlists  # home | playlists
  filters:
    category: true
    theme: true
    type: true
    decade: true
    emulator: true
    manufacturer: true
    favorites: true
  game:
    info: true
    help: true
    playfield: true
    highscore: true
    media:
      topper: false
      backglass: true
      fulldmd: true
      dmd: false
      playfield: true
      help: true
      info: true
  dateFormat: dd.MM.yyyy HH:mm

  # Available fields: year, type, manufacturer, numPlayers, emulator,
  #   category, theme, rating, lastPlayed, numPlays, timePlayed,
  #   designedBy, webLinkUrl, gameVersion, gameDescription, romName, tableType
  gameFields:
    - year
    - type
    - manufacturer
    - numPlayers
    - emulator
    - lastPlayed
    - numPlays
    - timePlayed
    - designedBy
    - webLinkUrl
    - gameVersion
    - rating

media:
  useThumbs: true
  cacheInMinutes: 20
  playfieldRotation: true
  # Overrides the wheel/thumbnail rotation reported by PinUP Popper's own
  # ThumbRotate setting. Leave as null to use PinUP Popper's value as-is.
  # Set to 0, 90, 180 or 270 if thumbnails still appear rotated incorrectly.
  thumbRotationOverride: null
  folders:
    topper: Topper
    backglass: BackGlass
    fulldmd: Menu
    dmd: DMD
    playfield: PlayField
    help: GameHelp
    info: GameInfo
    highscore: Other2
